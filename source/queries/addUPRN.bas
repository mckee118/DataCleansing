dbMemo "SQL" ="select a.UPCID_N_NVD_WP as addUPCID_N_NVD_WP, p.UPCID_N_NVD_WP as PUPCID_N_NVD_W"
    "P, p.UPRN into tempPointer from D_address a inner join POINTER p on a.UPCID_N_NV"
    "D_WP = p.UPCID_N_NVD_WP\015\012select addUPCID_N_NVD_WP, COUNT(*) as counter int"
    "o tempAddress from tempPointer group by addUPCID_N_NVD_WP having COUNT(*) >= 1\015"
    "\012Update main Set main.uprn = tp.uprn from D_address main inner join tempPoint"
    "er tp on main.UPCID_N_NVD_WP = tp.PUPCID_N_NVD_WP inner join tempAddress ta on m"
    "ain.UPCID_N_NVD_WP = ta.addUPCID_N_NVD_WP where main.uprn Is Null and len(UPCID_"
    "N_NVD_WP) > 1\015\012drop table tempPointer\015\012drop table tempAddress\015\012"
    "select a.UPCID_N_3C_WP as addUPCID_N_3C_WP, p.UPCID_N_3C_WP as PUPCID_N_3C_WP, p"
    ".UPRN into tempPointer from D_address a inner join POINTER p on a.UPCID_N_3C_WP "
    "= p.UPCID_N_3C_WP\015\012select addUPCID_N_3C_WP, COUNT(*) as counter into tempA"
    "ddress from tempPointer group by addUPCID_N_3C_WP having COUNT(*) >= 1\015\012Up"
    "date main Set main.uprn = tp.uprn from D_address main inner join tempPointer tp "
    "on main.UPCID_N_3C_WP = tp.PUPCID_N_3C_WP inner join tempAddress ta on main.UPCI"
    "D_N_3C_WP = ta.addUPCID_N_3C_WP where main.uprn Is Null and len(UPCID_N_3C_WP) >"
    " 1\015\012drop table tempPointer\015\012drop table tempAddress\015\012select a.U"
    "PCID_N_WP as addUPCID_N_WP, p.UPCID_N_WP as PUPCID_N_WP, p.UPRN into tempPointer"
    " from D_address a inner join POINTER p on a.UPCID_N_WP = p.UPCID_N_WP\015\012sel"
    "ect addUPCID_N_WP, COUNT(*) as counter into tempAddress from tempPointer group b"
    "y addUPCID_N_WP having COUNT(*) >= 1\015\012Update main Set main.uprn = tp.uprn "
    "from D_address main inner join tempPointer tp on main.UPCID_N_WP = tp.PUPCID_N_W"
    "P inner join tempAddress ta on main.UPCID_N_WP = ta.addUPCID_N_WP where main.upr"
    "n Is Null and len(UPCID_N_WP) > 1\015\012drop table tempPointer\015\012drop tabl"
    "e tempAddress\015\012select a.UPCID_WS_WP as addUPCID_WS_WP, p.UPCID_WS_WP as PU"
    "PCID_WS_WP, p.UPRN into tempPointer from D_address a inner join POINTER p on a.U"
    "PCID_WS_WP = p.UPCID_WS_WP\015\012select addUPCID_WS_WP, COUNT(*) as counter int"
    "o tempAddress from tempPointer group by addUPCID_WS_WP having COUNT(*) >= 1\015\012"
    "Update main Set main.uprn = tp.uprn from D_address main inner join tempPointer t"
    "p on main.UPCID_WS_WP = tp.PUPCID_WS_WP inner join tempAddress ta on main.UPCID_"
    "WS_WP = ta.addUPCID_WS_WP where main.uprn Is Null and len(UPCID_WS_WP) > 1\015\012"
    "drop table tempPointer\015\012drop table tempAddress\015\012"
dbMemo "Connect" ="ODBC;DSN=address;DATABASE=Address"
dbBoolean "ReturnsRecords" ="0"
dbInteger "ODBCTimeout" ="0"
dbBoolean "LogMessages" ="0"
dbByte "Orientation" ="0"
dbByte "PublishToWeb" ="1"
End
